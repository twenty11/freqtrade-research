# Architecture

8 层模块：strategy_dsl / validator / compiler / features / runner / evaluation / reporting / cli。

编译输出：`CompiledStrategy.py`、`runtime_config.json`、`compiled_meta.json`、`manifest.json`。

评估输出：summary、metrics、walk-forward、regime、sensitivity、lookahead、recursive、manifest。
